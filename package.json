{
  "name": "noterfly",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "check-types": "turbo run check-types",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "2.5.0"
  },
  "packageManager": "bun@1.2.8",
  "engines": {
    "node": ">=22"
  }
}
